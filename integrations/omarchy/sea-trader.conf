# Sea Trader — Hyprland settings (sourced from hyprland.conf by install.sh).
# The game window is launched by sea-trader-open with exec rules (float, size, centre) on the
# special workspace "seatrader", so no window rules are required.

# Optional keybinding to toggle the game window (uncomment and pick a free combo):
# bind = SUPER ALT, S, exec, ~/.local/bin/sea-trader-open
